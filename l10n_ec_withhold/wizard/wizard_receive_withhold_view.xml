<?xml version="1.0" encoding="utf-8"?>
<odoo>

	<record id="view_receive_withhold_form" model="ir.ui.view">
    	<field name="name">view.receive.withhold.form</field>
        <field name="model">wizard.receive.withhold</field>
        <field name="arch" type="xml">
        	<form string="Recibir retención">
        		<div>
        			<p>Desde esta opción se permite registrar una retención para una o múltiples facturas.</p>
        			<p>Tenga en cuenta que solo esta permitido para facturas de ventas, que se encuentren en estado abierto ó pagado y que pertenezcan al mismo cliente.</p>
        		</div>
                <footer>
                	<button name="action_receive" string="Recibir" type="object" class="btn-primary"/>
                    <button special="cancel" string="Cerrar" class="btn-default"/>
				</footer>
			</form>
		</field>
	</record>

	<act_window
		id="action_receive_withhold"
		name="Recibir retención"
		res_model="wizard.receive.withhold"
		binding_model="account.move"
		view_mode="form"
		target="new"/>

</odoo>
