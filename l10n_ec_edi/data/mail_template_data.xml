<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <record id="l10n_ec_email_template_edi_document" model="mail.template">
            <field name="name">EDI: Send by email</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="email_from">${(object.invoice_user_id.email_formatted or user.email_formatted) |safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="subject">${object.company_id.name} (${not object.l10n_latam_document_type_id and 'Document' or object.l10n_latam_document_type_id.report_name or object.l10n_latam_document_type_id.name}  ${object.name or 'n/a'})</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        Dear
        % if object.partner_id.parent_id:
            ${object.partner_id.name} (${object.partner_id.parent_id.name}),
        % else:
            ${object.partner_id.name},
        % endif
        <br /><br />
        Here is your
        % if object.name and object.l10n_latam_document_type_id:
            ${object.l10n_latam_document_type_id.report_name or object.l10n_latam_document_type_id.name} <strong>${object.name}</strong>
        % elif object.name:
            Invoice <strong>${object.name}</strong>
        % else:
            invoice
        %endif
        <br /><br />
        % if object.get_portal_url():
         <div style="margin: 16px 0px 16px 0px;">
            <a href="/my/invoices/${object.id}?access_token=${object.access_token}"
               style="background-color: #875A7B; padding: 8px 16px 8px 16px; text-decoration: none; color: #fff; border-radius: 5px; font-size:13px;">
                View Document
            </a>
         </div>
        % endif
        <br /><br />
        A regard from ${object.company_id.name}.
        <br /><br />
    </p>
</div>
            </field>
            <field name="report_template" ref="account.account_invoices"/>
            <field name="report_name">${(object.name or '').replace('/','_')}${object.state == 'draft' and '_draft' or ''}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>
